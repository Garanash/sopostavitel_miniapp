# Настройка Telegram Mini App

## Проблема: Мини-приложение не открывается

Если мини-приложение не открывается при нажатии на кнопку, попробуйте следующие решения:

## Решение 1: Настройка Menu Button в BotFather

1. Откройте [@BotFather](https://t.me/BotFather) в Telegram
2. Отправьте команду `/mybots`
3. Выберите вашего бота: `@sopostavitel_agb_bot`
4. Выберите **"Bot Settings"** → **"Menu Button"**
5. Выберите **"Configure menu button"**
6. Выберите **"Web App"**
7. Введите URL вашего мини-приложения:
   ```
   https://sail-span-manchester-definition.trycloudflare.com
   ```
8. Теперь кнопка меню в боте будет открывать мини-приложение автоматически!

## Решение 2: Проверка туннеля

Убедитесь, что туннель работает:

```bash
# Проверьте, запущен ли туннель
ps aux | grep cloudflared

# Если не запущен, запустите:
./start_tunnel.sh
```

## Решение 3: Использование команды /web

1. Откройте бота в Telegram
2. Отправьте команду `/web`
3. Нажмите на кнопку "🌐 Открыть мини-приложение"
4. Мини-приложение должно открыться

## Решение 4: Проверка URL

Убедитесь, что URL в `.env` правильный:

```bash
cat .env | grep WEB_APP_URL
```

Должно быть:
```
WEB_APP_URL=https://sail-span-manchester-definition.trycloudflare.com
```

## Решение 5: Альтернативный туннель (ngrok)

Если Cloudflare Tunnel не работает, используйте ngrok:

```bash
# Установите ngrok (если еще не установлен)
brew install ngrok

# Запустите туннель
ngrok http 3000

# Скопируйте HTTPS URL (например: https://abc123.ngrok-free.app)
# Обновите .env:
sed -i '' 's|WEB_APP_URL=.*|WEB_APP_URL=https://ВАШ_NGROK_URL.ngrok-free.app|' .env

# Перезапустите бота
pkill -f "python.*bot.py"
source venv/bin/activate
python bot.py &
```

## Текущий статус

- ✅ Бот запущен
- ✅ Веб-приложение работает на localhost:3000
- ✅ API работает на localhost:8000
- ⚠️ HTTPS туннель: https://sail-span-manchester-definition.trycloudflare.com

## Важно

Telegram требует **HTTPS** для мини-приложений. HTTP (localhost) не работает в Telegram, но работает в браузере.

Для тестирования в браузере используйте:
```
http://localhost:3000?user_id=ВАШ_ID
```

