# Бот для сопоставления артикулов

Telegram бот с веб-интерфейсом для сопоставления артикулов из базы данных с данными из файлов (счета, коммерческие предложения и т.д.).

## Возможности

- 🤖 Telegram бот для загрузки и обработки файлов
- 🌐 Веб-интерфейс (мини-приложение) для работы с артикулами
- 📄 Поддержка различных форматов: PDF, изображения (JPG, PNG), Excel, Word
- 🔍 OCR распознавание текста из изображений и PDF
- 📊 Сопоставление артикулов с контекстом и уровнем уверенности
- 💾 База данных для хранения артикулов и истории обработки

## Установка

### Требования

- Python 3.9+
- Node.js 18+
- Tesseract OCR (для распознавания текста)

### Установка Tesseract

**macOS:**
```bash
brew install tesseract
brew install tesseract-lang  # Для поддержки русского языка
```

**Linux (Ubuntu/Debian):**
```bash
sudo apt-get install tesseract-ocr
sudo apt-get install tesseract-ocr-rus
```

**Windows:**
Скачайте установщик с [GitHub](https://github.com/UB-Mannheim/tesseract/wiki)

### Установка Python зависимостей

```bash
pip install -r requirements.txt
```

### Установка Node.js зависимостей

```bash
cd web-app
npm install
```

## Настройка

1. Скопируйте `.env.example` в `.env`:
```bash
cp .env.example .env
```

2. Отредактируйте `.env` и укажите:
   - `TELEGRAM_BOT_TOKEN` - токен вашего Telegram бота (получите у [@BotFather](https://t.me/BotFather))
   - `TESSERACT_CMD` - путь к Tesseract (обычно `/usr/local/bin/tesseract` на macOS или `/usr/bin/tesseract` на Linux)

3. Для веб-приложения создайте `.env` в папке `web-app`:
```bash
cd web-app
cp .env.example .env
```

Отредактируйте `VITE_API_URL` если API работает на другом порту.

## Запуск

### Быстрый старт (все сервисы)

```bash
# Сделайте скрипты исполняемыми (если еще не сделано)
chmod +x start_all.sh run_bot.sh run_api.sh

# Запустите все сервисы
./start_all.sh
```

Затем в отдельном терминале запустите веб-приложение:
```bash
cd web-app
npm install  # Первый раз
npm run dev
```

### Пошаговый запуск

#### 1. Инициализация базы данных

```bash
python init_sample_data.py
```

Это создаст базу данных и добавит примеры артикулов для тестирования.

#### 2. Запуск API сервера

```bash
python api.py
```

Или с помощью uvicorn:
```bash
uvicorn api:app --host 0.0.0.0 --port 8000 --reload
```

API будет доступен по адресу: http://localhost:8000

#### 3. Запуск Telegram бота

В отдельном терминале:
```bash
python bot.py
```

Или используйте скрипт:
```bash
./run_bot.sh
```

#### 4. Запуск веб-приложения

В отдельном терминале:
```bash
cd web-app
npm install  # Только первый раз
npm run dev
```

Веб-приложение будет доступно по адресу: http://localhost:3000

## Использование

### Telegram бот

1. Найдите вашего бота в Telegram
2. Отправьте команду `/start`
3. Отправьте файл (PDF, изображение, Excel, Word)
4. Бот обработает файл и найдет артикулы из базы данных
5. Используйте `/web` для открытия веб-интерфейса

### Веб-интерфейс

1. Откройте веб-интерфейс через бота (команда `/web`) или напрямую
2. **Загрузка файлов**: Перетащите файл или выберите его для обработки
3. **Просмотр файлов**: Просмотрите историю обработанных файлов и результаты
4. **Управление артикулами**: Добавляйте, редактируйте и удаляйте артикулы в базе данных
5. **Статистика**: Просматривайте общую статистику системы

## Структура проекта

```
sopostavitel/
├── bot.py              # Telegram бот
├── api.py              # FastAPI сервер
├── database.py         # Модели базы данных
├── file_processor.py   # Обработка файлов и OCR
├── config.py           # Конфигурация
├── requirements.txt    # Python зависимости
├── web-app/            # React веб-приложение
│   ├── src/
│   │   ├── pages/      # Страницы приложения
│   │   └── App.jsx     # Главный компонент
│   └── package.json
└── README.md
```

## API Endpoints

### Артикулы
- `GET /api/articles` - Список артикулов
- `POST /api/articles` - Создать артикул
- `GET /api/articles/{id}` - Получить артикул
- `DELETE /api/articles/{id}` - Удалить артикул

### Файлы
- `POST /api/files/upload` - Загрузить и обработать файл
- `GET /api/files` - Список обработанных файлов
- `GET /api/files/{id}` - Детали файла

### Статистика
- `GET /api/stats` - Общая статистика

## База данных

Используется SQLite (по умолчанию) или можно настроить PostgreSQL через переменную окружения `DATABASE_URL`.

Таблицы:
- `articles` - Артикулы из базы данных
- `processed_files` - Обработанные файлы
- `matched_articles` - Найденные совпадения артикулов

## Поддерживаемые форматы

- **Изображения**: JPG, JPEG, PNG
- **Документы**: PDF
- **Таблицы**: XLSX, XLS
- **Текстовые документы**: DOCX

## Разработка

### Добавление новых типов файлов

Отредактируйте `file_processor.py` и добавьте новый метод обработки.

### Улучшение алгоритма сопоставления

Метод `extract_article_numbers` в `file_processor.py` можно улучшить для более точного поиска.

## Лицензия

MIT

## Поддержка

При возникновении проблем проверьте:
1. Установлен ли Tesseract и доступен ли он в PATH
2. Правильно ли указан токен бота в `.env`
3. Запущены ли все сервисы (API, бот, веб-приложение)

